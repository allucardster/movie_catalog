{% extends '::base.html.twig' %}

{% block body %}
    <div class="page-header">
        <h1>{% block current_breadcrumb %}Movie edit{% endblock %}</h1>
    </div>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('movie') }}">
            Back to the list
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
